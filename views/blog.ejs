<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create a New Blog</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            background-color: gray;
            padding: 0;
        }
        .container {
            max-width: 800px;
            margin: 50px auto;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
        }
        h1 {
            text-align: center;
            color: #333;
        }
        label {
            display: block;
            margin-top: 10px;
            font-weight: bold;
            color: #555;
        }
        #blogDescription{
            height: 80px;
            width: 100%;
        }
        input[type="text"],
        input[type="file"] {
            width: 98%;
            padding: 10px;
            margin-top: 5px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }
        button {
            width: 100%;
            padding: 15px;
            background-color: #007BFF;
            border: none;
            border-radius: 4px;
            color: white;
            font-size: 16px;
            cursor: pointer;
        }
        button:hover {
            background-color: #0056b3;
        }
        .form-group {
            margin-bottom: 15px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Create a New Blog</h1>
        <form action="/blog/createblog" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label for="blogName">Enter Blog Name:</label>
                <input type="text" id="blogName" name="blogName" required>
            </div>
            <div class="form-group">
                <label for="blogDescription">Enter Blog Description:</label>
                <textarea name="blogDescription" id="blogDescription"></textarea>
               
            </div>
            <div class="form-group">
                <label for="blogImageUrl">Select Image:</label>
                <input type="file" id="blogImageUrl" name="blogImageUrl" accept="image/*" required>
            </div>
            <button type="submit">Create Blog</button>
        </form>
    </div>
</body>
</html>
